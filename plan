경기 기록 사진에서 스탯 추출

1. 디스코드를 통해 사진 받기
    1-1. 디스코드 api key 발급 (o)
    1-2. 디스코드 봇으로 사진 받는 버튼 생성(o)
        -> 사진을 다운받을 건지 추후 선택 필요
    1-3. 받은 정보를 스프레드 시트 or 데이터베이스에 저장

2. 글자 판별(사진 구분) 방법 찾기
    2-1. 사진 구분 할 때 글자 추출 비교가 아닌, 사진 비교로 구분
    2-2. OCR은 숫자를 추출할 때만 사용할 예정

3. 사진 위치에 따라 분류
4. 특정 위치에 있는 글자 추출(OCR)
5. csv 혹은 엑셀 형태로 저장 후 파일 생성
