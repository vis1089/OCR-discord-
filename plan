경기 기록 사진에서 스탯 추출

1. 디스코드를 통해 사진 받기
    1-1. 디스코드 api key 발급 (o)
    1-2. 디스코드 봇으로 사진 받는 버튼 생성(o)
        -> 사진을 다운받을 건지 추후 선택 필요
    1-3. 포지션 선택 - 사용자로부터 포지션 정보 받기(o)
        -> 포지션 디비에서 사용자 아이디 검색
        -> 사용자가 없을 시, 새로 추가
    1-4. 받은 정보를 데이터베이스에 저장
        -> 데이터 베이스 구축 - FW 일부 구축
        -> FW, MF, DM, DF, FB, GK 테이블로 구성
        -> 우선 요약탭 활용

2. 글자 판별(사진 구분) 방법 찾기 - 다시 생각해봐야 할 부분 
    2-1. 사진 구분 할 때 글자 추출 비교가 아닌, 사진 비교로 구분
        -> 처음 나온 스탯을 보고 확인
        -> 우선, 요약 탭만을 활용할 예정
    2-2. OCR은 숫자를 추출할 때만 사용할 예정

3. 사진 위치에 따라 분류
4. 특정 위치에 있는 숫자 추출(OCR)
5. 숫자에 맞게 디비에 저장
